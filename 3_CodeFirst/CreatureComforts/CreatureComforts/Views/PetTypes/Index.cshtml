@model IEnumerable<CreatureComforts.Models.PetType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PetTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PetTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PetTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PetTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PetTypeID })
        </td>
    </tr>
}

</table>
